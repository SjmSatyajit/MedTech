SELECT 1;

-- =========================================================================
-- MedTech Database Schema Init
-- =========================================================================
-- NOTE: 
-- 1. Tables are automatically created/updated by Hibernate via the
--    'spring.jpa.hibernate.ddl-auto=update' property on startup.
-- 2. Stored procedures (get_nearby_resources) and triggers (after_booking_confirmed)
--    are created programmatically in com.medtech.config.DatabaseInitializer.
-- =========================================================================
